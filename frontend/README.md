# Frontend (Streamlit)

## Install
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

## Run
streamlit run app.py

## Notes
- Make sure backend is running at http://localhost:8000
- You can set the backend URL via Streamlit secrets or change BACKEND_URL in app.py
